spring:
  profiles:
    active: dev
  application:
    name: spring-jpa-review

  datasource:
    url: jdbc:mysql://localhost:3306/ecommerce
    username: root
    password: dev1990
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update # none, create-only, drop, create, create-drop, validate, update
    show-sql: true
    format_sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

kind: Application
